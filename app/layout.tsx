import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'VisaMate - Personal Immigration & Visa Assistant',
  description: 'Get accurate, source-based answers to your immigration and visa questions',
  keywords: 'immigration, visa, H-1B, F-1, OPT, USCIS, legal assistant',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
} 